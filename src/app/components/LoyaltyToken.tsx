

type LoyaltyTokenProps = {
  tokenUri: JSON; 
  showPoints?: boolean;
  showRequirements?: boolean;  
  showDesciption?: boolean; 
}

export const LoyaltyToken = (
  { tokenUri, 
    showPoints = false, 
    showRequirements = false,  
    showDesciption = false}: LoyaltyTokenProps
) => {

  return (
    <div> 


    </div>


  )
} 